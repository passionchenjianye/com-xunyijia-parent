server:
  port: 8082

spring:
  application:
    name: config-server
  cloud:
      config:
        server:
          git:
            uri: https://github.com/xunyijia/com-xunyijia-parent.git
            search-paths: xunyijia-config

eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8081/eureka/