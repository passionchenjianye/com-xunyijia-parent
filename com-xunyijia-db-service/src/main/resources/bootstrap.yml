server:
  port: 8084

spring:
  cloud:
    config:
      label: dev
      uri: http://localhost:8082/
      profile: dev
      name: config-server
  application:
      name: db-service

eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8081/eureka/